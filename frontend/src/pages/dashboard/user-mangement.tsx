import * as React from 'react';
import { Typography } from '@mui/material';

export default function UserManagement() {
    return (
        <Typography variant="h4">UserManagement Page</Typography>
    );
}