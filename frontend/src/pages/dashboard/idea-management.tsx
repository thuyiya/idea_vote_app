import * as React from 'react';
import { Typography } from '@mui/material';

export default function IdeaManagement() {
    return (
        <Typography variant="h4">IdeaManagement Page</Typography>
    );
}